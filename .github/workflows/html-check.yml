name: HTML basic Workflow

on: 
  push:
    branches: 
      - main

jobs:
  html-check:
    runs-on: ubuntu-latest
    steps:
      - name: show index.html content
        run: cat index.html

      - name: print success message
        run: echo "Github-actions workflow successfull"
      
    
